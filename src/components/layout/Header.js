import './Header.scss'

export default function Header() {
    return (
        <header className='header'>
            <h1>Posts by react and redux toolkit</h1>
            <p>
                simple project to use redux toolkit to fetch posts
            </p>
        </header>
    )
}